<Window x:Class="RealTimeTranslator.UI.Views.SettingsWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       Title="設定"
       Width="700" Height="600"
       MinWidth="600" MinHeight="500"
       WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="翻訳設定" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="ソース言語"/>
                        <ComboBox Grid.Row="0" Grid.Column="1"
                                  ItemsSource="{Binding Languages}"
                                  SelectedItem="{Binding Settings.Translation.SourceLanguage}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="ターゲット言語" Margin="0,6,0,0"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,6,0,0"
                                  ItemsSource="{Binding Languages}"
                                  SelectedItem="{Binding Settings.Translation.TargetLanguage}"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="オーバーレイ設定" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="フォント"/>
                        <ComboBox Grid.Row="0" Grid.Column="1"
                                  ItemsSource="{Binding FontFamilies}"
                                  SelectedItem="{Binding Settings.Overlay.FontFamily}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="フォントサイズ" Margin="0,6,0,0"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,6,0,0"
                                  ItemsSource="{Binding FontSizes}"
                                  SelectedItem="{Binding Settings.Overlay.FontSize}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="仮字幕色" Margin="0,6,0,0"/>
                        <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,6,0,0"
                                  ItemsSource="{Binding TextColorOptions}"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="Value"
                                  SelectedValue="{Binding Settings.Overlay.PartialTextColor}"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="確定字幕色" Margin="0,6,0,0"/>
                        <ComboBox Grid.Row="3" Grid.Column="1" Margin="0,6,0,0"
                                  ItemsSource="{Binding TextColorOptions}"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="Value"
                                  SelectedValue="{Binding Settings.Overlay.FinalTextColor}"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="背景色" Margin="0,6,0,0"/>
                        <ComboBox Grid.Row="4" Grid.Column="1" Margin="0,6,0,0"
                                  ItemsSource="{Binding BackgroundColorOptions}"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="Value"
                                  SelectedValue="{Binding Settings.Overlay.BackgroundColor}"/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="最大行数" Margin="0,6,0,0"/>
                        <ComboBox Grid.Row="5" Grid.Column="1" Margin="0,6,0,0"
                                  ItemsSource="{Binding MaxLinesList}"
                                  SelectedItem="{Binding Settings.Overlay.MaxLines}"/>
                    </Grid>
                </GroupBox>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="保存" Width="120" Command="{Binding SaveAsyncCommand}"/>
            <Button Content="閉じる" Width="120" Margin="6,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
