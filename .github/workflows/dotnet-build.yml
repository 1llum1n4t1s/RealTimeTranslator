name: .NET ビルド

on:
  pull_request:
    branches-ignore:
      - releases

jobs:
  build:
    name: .NET ビルド
    runs-on: windows-latest

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: .NET SDK をセットアップ
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: 依存関係を復元
        run: dotnet restore RealTimeTranslator.slnx

      - name: ビルド
        run: dotnet build RealTimeTranslator.slnx --configuration Release --no-restore -p:Platform=x64

      - name: テスト実行
        run: dotnet test RealTimeTranslator.slnx --configuration Release --no-build -p:Platform=x64 --verbosity normal --logger "trx;LogFileName=test-results.trx"
        continue-on-error: true

      - name: テスト結果をアップロード
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-Release
          path: '**/TestResults/*.trx'
          retention-days: 30

      - name: ビルド成果物をアップロード
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-release
          path: |
            src/**/bin/Release/**/*.exe
            src/**/bin/Release/**/*.dll
            !src/**/bin/Release/**/ref/**
          retention-days: 7
